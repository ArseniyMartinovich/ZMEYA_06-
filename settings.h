#pragma once
#include <iostream>
#include "SFML/Graphics.hpp"

const float WINDOW_WIDTH{ 1000.f };
const float WINDOW_HEIGHT{ 1000.f };
const size_t FPS{ 60 };

const int ZMEYA_START_POS = (WINDOW_WIDTH / 2, WINDOW_HEIGHT / 2);

const std::string PLAYER_FILE_NAME = "head.png";